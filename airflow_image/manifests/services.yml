apiVersion: v1
kind: Service
metadata:
  name: airflow-webserver
  namespace: default
spec:
  selector:
    app: airflow
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  type: NodePort
