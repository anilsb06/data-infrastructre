apiVersion: v1
kind: Service
metadata:
  name: airflow-deployment-service
  namespace: default
spec:
  selector:
    app: airflow
  ports:
  - port: 443
    protocol: TCP
    targetPort: 8080
  type: LoadBalancer