apiVersion: v1
kind: Service
metadata:
  name: airflow-webserver-staging
  namespace: default
spec:
  selector:
    app: airflow
  ports:
  - port: 8081
    protocol: TCP
    targetPort: 8081
  type: NodePort
